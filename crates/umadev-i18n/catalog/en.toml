# UmaDev English UI strings.
# Keys must match zh-CN.toml / zh-TW.toml exactly (enforced by the parity test).

# First run — language selection
"lang.picker.title" = "Choose interface language"
"lang.picker.hint" = "↑↓ select · Enter confirm · change anytime with /lang"
"lang.changed" = "Interface language switched to {}"
"lang.current" = "Current interface language: {}"
"lang.unknown" = "Unknown language. Options: zh-CN (Simplified) / zh-TW (Traditional) / en (English)"

# First run — welcome and configuration
"firstrun.welcome" = "Welcome to UmaDev — your AI delivery director for commercial-grade software"
"firstrun.configure" = "First run: a few quick settings (all changeable later while you work)"

# Worker / backend selection
"picker.title" = "Choose your worker base (UmaDev borrows its model as the brain)"
"picker.hint" = "↑↓ select · Enter confirm"
"picker.confirm" = "Selected: {}"
"picker.footer" = "↑↓ select · Enter confirm · Esc quit   ·   [ok]=installed (login not verified; if a run reports not-logged-in, run that CLI once in your terminal to log in) · No AI tools? Pick \"External model/API\" and use your own key"
"backend.claude" = "Claude Code (claude --print, uses your logged-in subscription)"
"backend.codex" = "Codex CLI (codex exec)"
"backend.opencode" = "OpenCode (opencode run)"
"backend.offline" = "Offline templates (no AI, demo / CI)"

# Common
"common.confirm" = "Confirm"
"common.cancel" = "Cancel"
"common.continue" = "Continue"
"common.back" = "Back"

# Input box placeholders (always visible)
"input.gate" = "Type revision, or /continue to approve…"
"input.finished" = "New requirement, or /quit…"
"input.running" = "running — type to queue a message (sent at the next gap); Esc to interrupt"
"model.synced" = "synced driving model {} (from {}) — the agent runs on the base's own model"
"model.synced_default" = "{} pins no model in config — the agent runs on its login/default model (the real model shows after a run)"
"model.reasoning" = "reasoning effort {} (from the base config; UmaDev does not override)"
"input.idle" = "Ask anything…  (/help for commands)"
"input.aborted" = "this round was aborted — type a new requirement to try again"

# Status
"status.ready" = "Ready"
"status.aborted" = "this round was aborted"
"status.stopping" = "stopping…"
"status.thinking" = "thinking…"
"status.using_tool" = "using"
"status.act.reading" = "reading"
"status.act.editing" = "editing"
"status.act.running" = "running"
"status.act.searching" = "searching"
"status.act.fetching" = "fetching"
"status.esc_cancel" = "esc to cancel"
"status.esc_confirm" = "esc again to interrupt"
"mode.auto_chip" = "auto-approve (shift+Tab for manual)"
"mode.manual_chip" = "manual review (shift+Tab for auto)"
"mode.plan_chip" = "plan · read-only (/mode to switch)"
"mode.guarded_chip" = "guarded · review each gate (/mode to switch)"
"mode.plan.on" = "[ok] Plan mode: on (read-only). Only research + PRD/architecture/UIUX planning docs are produced; the pipeline stops at the docs checkpoint and does NOT execute spec/frontend/backend — no real code is written. Once you're happy, run /mode guarded or /mode auto to execute."
"mode.guarded.on" = "[ok] Guarded mode: on (default). Every checkpoint pauses for your review; type c to approve, or text to revise. Actions touching .git / the network / destructive shell always require confirmation."
"mode.auto.on" = "[ok] Autonomous mode: on. Checkpoints auto-approve and deliver end-to-end. Irreversible actions (.git / network / destructive shell) still ask for your confirmation — a safety floor that auto mode does not bypass."
"mode.current" = "Current trust mode: {}. Switch with /mode plan|guarded|auto (plan = read-only plan, guarded = review each gate, auto = fully autonomous)."
"mode.unknown" = "Unknown mode \"{}\". Options: plan (read-only plan) / guarded (review each gate, default) / auto (fully autonomous)."
"mode.plan.gate" = "[plan] Plan mode (read-only): the pipeline stops here — no later phases ran and no real code was written. Once you're happy with the plan above, re-enter the requirement under /mode guarded or /mode auto to execute."
"trust.suggest_auto" = "[trust] You've approved {} times in a row at the `{}` gate — want to let it auto-advance? Type /mode auto for fully autonomous, or keep reviewing manually (this hint shows once and never changes your default automatically)."
"trust.reason.reversible" = "reversible in-project edit"
"trust.reason.git" = "touches .git internals (can rewrite history)"
"trust.reason.network" = "reaches the network (push/pull/download)"
"trust.reason.destructive" = "destructive / irreversible shell action"
"trust.cap.read" = "read (auto)"
"trust.cap.write" = "edit files (guarded)"
"trust.cap.shell" = "run shell commands (confirm)"
"trust.cap.network" = "network access (allowlist)"
"trust.branch_isolated" = "[trust] Working on an isolated branch `{}` (derived from `{}`) — your branch is untouched; review and merge it yourself when you're happy. UmaDev never auto-merges or pushes."
"chat.build_detected" = "[build] This turn is writing real files — switching into a build: holding the single-writer lock and isolating the work. For the full plan + team + delivery flow, use /run."
"trust.circuit_open" = "[trust] Paused: {} failures in quick succession — something is repeatedly going wrong. Review the log and retry when ready (this safety brake stops a run from burning effort in a failure loop)."
"recovery.title" = "This run stopped — pick a next step"
"recovery.hint" = "↑/↓ to choose · Enter to run · Esc to dismiss"
"recovery.retry" = "Retry the run"
"recovery.switch_base" = "Switch base CLI"
"recovery.open_log" = "Open the run log"
"recovery.run_doctor" = "Run doctor (diagnose environment)"
"recovery.show_login" = "Show the base login command"
"recovery.rollback" = "Roll back this run's changes"
"status.running" = "Running"
"status.paused" = "Paused, awaiting confirmation"

# Gate review cards
"gate.clarify.title" = "clarify — answer the questions so the base understands what you really want"
"gate.clarify.checklist" = "Answer each question (or write 'skip')\nUnsure? write 'you decide'\nThe more specific, the better the PRD/code fits"
"gate.clarify.next" = "When done, type c to continue (into web research), or describe changes"
"gate.docs.title" = "docs_confirm — three core docs are ready for review"
"gate.docs.checklist" = "PRD: do goals / scope / acceptance criteria cover what you actually want?\nArchitecture: are the API, data model and tech choices sound?\nDesign: do colors / fonts / component tokens match expectations, is there dark mode?"
"gate.docs.next" = "Not sure? press c to trust the AI's plan. Note: typing ANY text re-runs the whole block per your change (minutes; /cancel to stop); to just approve, type nothing."
"gate.preview.title" = "preview_confirm — frontend preview is ready for review"
"gate.preview.checklist" = "Does the frontend follow the UIUX design system (icon lib / tokens / typography)?\nDo the page structure and flows match the PRD user stories?\nDo the fetch URLs match the architecture doc's API?"
"gate.preview.next" = "Review the runnable frontend preview. Press c to proceed to backend / quality / delivery; typing text re-runs the frontend per your change (minutes; /cancel to undo)."
"gate.artifacts_header" = "Artifacts:"
"gate.quality_warn_header" = "[warn] Quality notes:"
"gate.scaffold_warn" = "{}: offline scaffold — worker timed out or unconfigured, use /revise to regenerate"
"gate.short_warn" = "{}: only {} lines — may be incomplete"
"gate.suggest_revise" = "Tip: use /revise 'regenerate architecture' to have the worker fill it in"
"gate.quality_line" = "Quality: {} CSS tokens · dark mode: {}"
"gate.detail.missing" = "[warn] MISSING"
"gate.detail.scaffold" = "{} lines [warn] SCAFFOLD"
"gate.detail.short" = "{} lines [warn] SHORT"
"gate.detail.ok" = "{} lines [ok]"
"gate.detail.dark_ok" = "[ok]"
"gate.detail.dark_missing" = "[fail] missing"
"gate.checklist_header" = "Approval checklist (confirm these before approving):"
"gate.actions_header" = "Actions:"
"gate.action.continue" = "/continue or c         -> approve gate"
"gate.action.revise" = "/revise <notes>        -> worker redoes it"
"gate.action.diff_prd" = "/diff prd              -> view the PRD"
"gate.action.diff_arch" = "/diff architecture     -> view the architecture"
"gate.action.diff_uiux" = "/diff uiux             -> view the design system"
"gate.guide_prefix" = "Guide: {}"
"gate.auto_approved" = "[auto] Autonomous mode: auto-approved checkpoint `{}`, continuing…  (type /manual to review each step)"

# Runtime narration
"run.started" = "Pipeline started: {}\nDelivering in 9 phases; it pauses at key checkpoints for your review."
"run.phase_start" = "> phase {} starting…"
"run.artifact_written" = "  · [{}] wrote {}"

# Wave 1 — intent pre-commitment card (router decision shown before any work)
"intent.build" = "This is a full build ({}) — I'll run the director flow: plan, team, quality gates."
"intent.debug" = "This is a defect to diagnose and fix ({}) — I'll locate, fix, and regress it."
"intent.quick_edit" = "Small, well-scoped change ({}) — on it directly with a targeted check."
"intent.explain" = "A read-only explanation ({}) — I'll answer without touching the workspace."
"intent.chat" = "This is a conversation ({}) — replying directly, no dev flow."
"intent.depth.fast" = "fast"
"intent.depth.standard" = "standard"
"intent.depth.deep" = "deep"
"intent.budget" = " · est. ≤{} tool calls"
"intent.team" = " · team: {}"

# Wave 1 — live plan checklist + team-review panel
"attach.image" = "Image"
"model.unmanaged" = "UmaDev does not manage the model — the base runs whatever it is configured / logged in with (an official subscription, or your own third-party / local model wired into the base). To change it, configure the model in the base (claude / codex / opencode); UmaDev just calls it."
"plan.synthesizing" = "Planning the delivery — drafting the steps for this requirement (a complex one may take a moment)…"
"plan.posted" = "Plan posted — {} steps. Track them in the checklist; steer with /plan."
"plan.panel.title" = "Plan"
"plan.panel.collapsed" = "Plan {}/{} — /plan to expand"
"plan.panel.active_step" = "step {}/{} · running"
"plan.panel.more_hint" = "scroll up or /plan for all"
"plan.none" = "No active plan. A build or a deliberate turn posts one here."
"plan.review.title" = "Team review"
"plan.review.accept" = "accepts"
"plan.review.block" = "{} must-fix"
"plan.review.collapsed" = "Team review: {} accept · {} blocking — /plan to expand"
"plan.review.note.accept" = "Team review · [{}] accepts"
"plan.review.note.block" = "Team review · [{}] {} must-fix:"
"plan.review.note.summary" = "Team review settled — {} accept · {} blocking (full verdicts above)."
"plan.review.section" = "Team review — {} accept · {} blocking"
"plan.steer.usage" = "/plan — show plan · /plan skip <id> · /plan add <text> · /plan veto <id> · /plan up <id> · /plan down <id> · /plan collapse"
"plan.steer.skip" = "Skipping step `{}` — folded into the next directive."
"plan.steer.veto" = "Vetoing step `{}` — folded into the next directive."
"plan.steer.add" = "Adding step: {} — folded into the next directive."
"plan.steer.move" = "Reordering step `{}` ({}) — folded into the next directive."
"plan.steer.unknown_step" = "No step `{}` in the current plan."
"plan.steer.queued" = "Plan edit queued; it applies at the next step boundary."

# Team (/team) — surface the AI development team as a first-class concept
"team.title" = "Your AI development team"
"team.roster.pm" = "Product manager — produces output/*-prd.md (the PRD + acceptance criteria)"
"team.roster.architect" = "Architect — produces output/*-architecture.md + .umadev/contracts/openapi.* (the API contract)"
"team.roster.designer" = "UI/UX designer — produces output/*-uiux.md + design tokens (the design system)"
"team.roster.frontend" = "Frontend engineer — builds the components / pages"
"team.roster.backend" = "Backend engineer — builds the API + data model + migrations"
"team.roster.qa" = "QA — produces runtime-proof.json + the tests"
"team.roster.security" = "Security engineer — owns the security baseline / SAST"
"team.roster.devops" = "DevOps — produces deploy-proof.json"
"team.roster.coordinator" = "Tech lead · coordinator — schedules the team, owns the plan, enforces the gates (the glue)"
"team.run.header" = "This run's team"
"team.run.deliverables" = "Deliverables"
"team.run.produced" = "[produced]"
"team.run.pending" = "[pending]"
"team.deliverable.prd" = "PRD (output/*-prd.md)"
"team.deliverable.architecture" = "Architecture (output/*-architecture.md)"
"team.deliverable.uiux" = "UI/UX (output/*-uiux.md)"
"team.deliverable.contract" = "API contract (.umadev/contracts/openapi.*)"
"team.deliverable.runtime" = "Runtime proof (runtime-proof.json)"
"team.deliverable.deploy" = "Deploy proof (deploy-proof.json)"
"team.no_run" = "No build is running yet — the team convenes on a build. Start one with /run, and this fills in with who was summoned, their verdicts, and the deliverables they produced."

# Help
"help.title" = "UmaDev commands"
"help.lang" = "/lang — switch interface language (Simplified / Traditional / English)"

# Provider / preview / connectivity
"preview.already_running" = "Preview already running, reopening browser: {}"
"preview.stopped" = "Preview dev server stopped."
"preview.none_running" = "No preview server is running."
"init.failed" = "/init failed: {}"
"design.applied" = "Design system: `{}`\n{}"
"report.write_failed" = "[warn] could not write report: {}"

# Greeting (cold-start first screen)
"greeting.main" = "UmaDev — turn a one-line idea into a shippable, commercial product.\nYou write zero code: I drive your logged-in AI worker through research -> design -> build -> preview -> ship, auto-applying a commercial design system and leaving quality + audit evidence.\nCurrent worker: {}"
"greeting.examples" = "Just type what you want to build, for example (feel free to copy one):\n\n  · A SaaS analytics dashboard with email login\n  · A to-do app with groups and reminders\n  · A blog system with Markdown authoring\n  · A product landing page ready to ship\n\nNext: answer a few clarifying questions -> review the three design docs (c to approve) -> /preview to see it -> /deploy to ship.\nMore: /help all commands · /design change design style (auto-picked by default) · /config current settings"

# Provider preset labels (vendor menu in the external-API wizard)

# External model/API setup wizard

# Resume / session hints
"session.completed" = "[session] This workspace finished its last pipeline run (requirement: \"{}\").\n  /diff to view the last output · or just type a new requirement to start fresh."
"session.paused_at_gate" = "[session] This workspace paused at gate `{}` last session (requirement: \"{}\").\n  /continue to proceed · /diff to view output · or start a new requirement (resets workflow state)."
"session.unfinished" = "[session] Detected an unfinished session (phase={}, requirement: \"{}\").\n  Type /continue to resume · a new requirement restarts from scratch · /status for details"
"session.resume_goal" = "[goal] Last time a goal was left unfinished: next step \"{}\" (step {}/{} done). /run to pick it back up · or just say whether to continue."

# Codex high-risk sandbox liability warning (only shown for danger-full-access on codex)
"codex.sandbox.danger_warning" = "WARNING: Codex high-risk sandbox (danger-full-access) is ENABLED — you assume all risk to your system environment.\n  To revert: set [codex] sandbox_mode = \"workspace-write\" in .umadevrc, then restart."

# Shared config write error
"config.write_failed" = "(could not write {}: {})"
"config.save_failed_note" = "[warn] Setting applied for this session, but could not be saved (it will revert on next launch): {}"

# Chitchat replies (offline keyword heuristic)
"chitchat.greeting" = "Hi! I'm UmaDev — your AI coding project-director Agent.\n\nJust type the product you want and I'll start the 9-phase pipeline:\n  research -> docs -> spec -> frontend -> backend -> quality -> delivery\n\nFor example:\n  A SaaS landing page with email login\n  A to-do app\n  A blog system"
"chitchat.thanks" = "You're welcome! Send a requirement anytime."
"chitchat.who" = "I'm UmaDev — the project-director Agent for AI coding.\n\nI drive your logged-in claude code / codex / opencode through a 9-phase commercial delivery flow\nto turn a one-line idea into a complete product (docs + frontend + backend + deploy).\n\nJust type a requirement to start, for example:\n  A forum with user registration"
"chitchat.fallback" = "Got it! If you want to build a product, just describe the requirement to start the pipeline.\n\nFor example:\n  A SaaS landing page with email login\n  A to-do app\n\nOr type /help for all commands."
"chat.claims_unverified" = "[warn] The reply above is chat only — no files were changed this turn. Verify any claimed edits against the actual files / `git status`."
"chat.director_build_with_history" = "## Conversation so far\n{}\n\n---\n\n{}"

# Delivery completion banner
"delivery.latest_zip" = "\n  latest: {} ({} KiB)"
"delivery.scorecard" = "\n  scorecard (shareable): release/{}"
"delivery.complete_banner" = "[ok] Pipeline complete, your product is ready! {}\n\nGo live:\n  · /preview -> run it locally to see the real thing\n  · /deploy -> ship it (runs the deploy command the base recorded, returns a live URL)\n\nProof (shareable with team / client / audit):\n  · open the scorecard.html above -> independent quality score + governance + compliance + tamper-proof hash\n  · /export -> view deliverables / proof-pack details\n\nMore:\n  · /redo -> re-run the current requirement · type a new requirement -> start fresh\n  · /status -> view full status"
"delivery.preview_line" = "\n  preview: {} (/preview to start it locally)"

# Build-complete card (every effective build: chat / Fast / Delivery)
"build.complete.title" = "✅ Build complete"
"build.complete.files" = "**Files changed** ({}):\n{}"
"build.complete.files_more" = "\n  …and {} more"
"build.complete.no_files" = "**Output dir**: {}"
"build.complete.entry" = "**Key entry**: {}"
"build.complete.run" = "**Run command**: `{}`"
"build.complete.preview_starting" = "**Preview**: starting dev server… the URL appears once it's ready"
"build.complete.preview_line" = "**Preview**: {}\n  (open it in your browser; /stop-preview to stop)"

# Build/verify failure next-step hints
"verify.action.tool_missing" = "-> Build tool missing. Run /doctor to check the environment, or install the missing dependency and /redo."
"verify.action.deps_missing" = "-> Dependencies not installed. Install them first (npm/pnpm install, cargo fetch), then /redo."
"verify.action.type_error" = "-> Type/syntax error. Use /revise \"fix the build error\" to have the worker fix it, or edit by hand and /redo."
"verify.action.generic" = "-> Use /revise \"fix the build error\" to have the worker fix it, or check the worker output above to locate the problem."

# Picker / quit / gate confirmation / run lifecycle
"picker.unavailable" = "{} unavailable: {} — pick another above, or install the base first and come back"
# Wave 1 — honest three-state login readiness in the picker (gap G10)
"picker.auth.logged_in" = "logged in"
"picker.auth.not_logged_in" = "installed · not logged in -> {}"
"picker.auth.not_installed" = "not installed -> {}"
"picker.auth.unknown" = "installed · login not verified"
"picker.block.not_logged_in" = "{} is installed but not logged in. Run `{}` in your terminal first, then come back."
"picker.block.not_installed" = "{} is not installed. Run `{}` first, then re-open UmaDev."
"quit.confirm_running" = "Press Esc again to quit UmaDev (workflow state is saved; the next `umadev` resumes automatically)."
"quit.use_command" = "Ctrl+C no longer quits (so a copy reflex can't drop the session); use /quit or /exit to exit — workflow state is saved and resumes next time."
"gate.clarify_saved" = "[ok] Clarification answers saved, starting web research…"
"gate.clarify_recorded" = "Recorded (keep answering, or type c to submit)"
"gate.clarify_write_failed" = "[warn] Could not save that answer ({}) — it was NOT recorded; please retype it."
"gate.confirmed_docs" = "Three core docs approved, generating spec and frontend…"
"gate.confirmed_preview" = "Frontend preview approved, building the backend…"
"gate.confirmed_generic" = "Approved, proceeding…"
"gate.revision_received" = "Revision received: \"{}\". Re-running the current block…"
"run.queued" = "[queued] Queued — it will be sent to the base as a revision at the next gap (gate / phase boundary). Press Esc to interrupt the current phase."
"chat.queued" = "[queued] Queued — the base is still replying; this message will be sent the moment it finishes."
"chat.interrupted" = " [interrupted] reply incomplete"
"chat.restored" = "[chat] Restored your last conversation ({} messages). Pick up where you left off, or /sessions to switch, /clear to start fresh."
"sessions.empty" = "[chat] No saved chats for this project yet. They save automatically as you talk; reopen one with /resume <id>."
"sessions.header" = "[chat] Saved chats (* = current):\n{}  Reopen one with /resume <id>."
"resume.usage" = "Usage: /resume <id> — run /sessions first to see resumable chat ids."
"resume.not_found" = "[chat] No chat `{}` (it may be deleted, or the id is wrong). /sessions lists what's available."
"resume.done" = "[chat] Reopened chat `{}` ({} messages); the base will carry on with this context."
"compact.too_short" = "[compact] The conversation is still short — nothing to compact yet."
"compact.summary" = "[Summary of earlier conversation · {} messages]\n{}"
"compact.done" = "[compact] Folded the conversation into a summary: {} -> {} messages — frees context while keeping the earlier points."
"run.steer_applied" = "[steer] Applying your queued message — redoing the current phase with it…"
"run.queued_unsent" = "[note] The pipeline finished before your queued message \"{}\" reached a gate, so it was not sent — just send it again."
"run.classified_build" = "-> Classified as a build task · entering the 9-phase pipeline (/cancel to stop mid-way)"
"design.suggest_hint" = "Tip: you haven't picked a design system. Use /design <name> to lock the visual direction before the run:\n  {}.\n  Skipping is fine — the worker will auto-pick one based on the requirement."
"run.preflight_plan" = "Requirement received: \"{}\"\n\nConfig: worker `{}` · design system `{}` · template `{}`\n\n9-phase pipeline:\n  1) research (competitors + discovery) -> 2) docs (PRD + Architecture + UIUX) -> [gate] docs_confirm\n  3) spec -> 4) frontend (built from UIUX tokens) -> [gate] preview_confirm\n  5) backend -> 6) quality (5-dimension review) -> 7) delivery -> proof-pack\n\nThe two gates auto-approve by default (they show as checkpoints only); press shift+Tab for manual per-gate review. Starting the pipeline…"
"run.cancelled" = "[cancelled] The current run is stopped, back to the prompt. Artifacts from completed phases stay in output/; you can /continue or type a new requirement next time."

# /init manifest
"init.design_files" = "\n  design infrastructure: {} files written to knowledge/"
"init.manifest_written" = "[ok] umadev.yaml written to {}\n  level={} profile={} slug={}{}"
"init.design_files_new" = "design infrastructure: {} new files written to knowledge/"
"init.already_exists" = "umadev.yaml already exists. Design files are ready too."

# /continue and /revise hints
"continue.running" = "The pipeline is running — wait for the next gate to pause, then /continue."
"continue.finished" = "The pipeline is complete. Type a new requirement to start fresh, or /quit to exit."
"continue.not_started" = "No pipeline started yet. Just type a one-line requirement to launch the 9-phase pipeline."
"continue.resuming" = "Resuming your previous run — re-attaching to the saved plan and picking up the remaining steps (the finished ones stay done)."
"continue.resuming_chat" = "Resuming the previous conversation — the base picks up with full context."
"continue.chat_directive" = "Continue the work from where we left off."
"continue.resuming_plan" = "team · resuming the saved plan ({}/{} steps already done) — driving only what's left"
"revise.usage" = "Usage: /revise <revision notes>.\n  Example: /revise drop social login, keep only email + password"
"revise.no_gate" = "No gate is open, nothing to revise — to change output, wait for the pipeline to pause at a gate (docs_confirm / preview_confirm)."

# Pitfalls overlay / cancel / unknown command
"pitfalls.overlay_title" = " Pitfall knowledge base — Esc to close, ↑↓ to scroll "
"cancel.none_running" = "No pipeline is running — nothing to cancel. Type a requirement to start fresh, or /quit to exit."
"slash.did_you_mean" = " did you mean `/{}`?"
"slash.unknown" = "Unknown command `/{}`.{} Type /help for the full list."

# Common (extras)
"common.none" = "(none)"

# /backend and /model
"backend.switched" = "Switched worker to `{}` — the next run uses this backend."
"backend.busy_no_switch" = "A run is in progress on the current base — /cancel it before switching the base, so the run and its session stay coherent."
"model.base_default" = "(base default)"
"model.menu.claude" = "  Claude (alias or full id both work):\n    · /model opus —— strongest  · /model sonnet —— balanced (default)  · /model haiku —— fastest\n    · full ids like claude-opus-4-8 / claude-sonnet-4-6"
"model.menu.codex" = "  Codex (OpenAI model id):\n    · /model gpt-5.1-codex  · /model gpt-5  · /model o4-mini"
"model.menu.opencode" = "  OpenCode (must be provider/model form):\n    · /model anthropic/claude-sonnet-4-6  · /model openai/gpt-5"
"model.menu.external" = "  Running offline templates; /model only affects the base CLIs (claude-code/codex/opencode)."
"model.current" = "Current model: {}\nSwitch: /model <id>\n{}\n  Note: you must use an id the base supports; a wrong id errors at the base on the next run."
"model.switched" = "Model switched to `{}` — the next run uses this model."


# /design and /template
"design.not_found" = "Design system `{}` not found. Options: {}"
"template.usage" = "Usage: /template <name>. Current: {}.\n  Options: {}\n  Example: /template saas-landing · /template dashboard · /template blog-content"
"template.not_found" = "Template `{}` not found. Options: {}"
"template.switched" = "Seed template switched to `{}` — the next run's frontend phase will follow this template's page structure and quality bar."

# /run and /redo
"run.busy_reopen" = "A pipeline is running — /cancel it before starting again, to avoid two pipelines writing the same workspace."
"run.usage" = "Usage: /run [slug] <requirement>\n  Example: /run my-app build a login system\n  Example: /run build a todo list (slug auto-uses the directory name)"
"goal.busy_reopen" = "A task is already running — /cancel it before /goal, to avoid two tasks writing the same workspace."
"goal.usage" = "Usage: /goal <objective>\n  Example: /goal build a shippable todo app with login and reminders\n  Set a goal and I'll lead the team toward it until it's met (using the base's native persistent /goal when it supports one)."
"goal.starting" = "[goal] Goal received: \"{}\". I'll drive the team toward it until it's done and the build/quality checks pass (/cancel to stop mid-way)."
"run.slug_invalid" = "slug `{}` contains illegal characters. Only letters, digits, `-`, `_` are allowed."
"redo.busy" = "A pipeline is running — /cancel it before /redo, to avoid two pipelines writing the same workspace."
"redo.no_requirement" = "No requirement has been run yet — just type a requirement or use /run to start."
"redo.rerunning" = "Re-running requirement: \"{}\". The pipeline restarts from research…"

# /quick — lightweight fast track
"quick.busy" = "A pipeline is running — /cancel it before /quick, to avoid two pipelines writing the same workspace."
"quick.usage" = "Usage: /quick <task>\n  Example: /quick tweak the header copy\n  Lightweight track: clarify-lite → implement → verify (skips research / 3 docs / both gates / delivery)."
"quick.starting" = "[light] Lightweight track: \"{}\". Running spec → implement → quality (no gates)…"

# /redo <phase> — re-run a single phase
"redo.phase.usage" = "Usage: /redo [phase]\n  /redo            re-run the whole requirement from scratch\n  /redo <phase>    re-run ONE phase (e.g. /redo frontend) — handy for a base that went offline\n  Valid phases: {}"
"redo.phase.unknown" = "Unknown phase `{}`. Valid phases: {}"
"redo.phase.no_run" = "No prior run found — start one with a requirement (or /run) before re-running a single phase."
"redo.phase.rerunning" = "Re-running just the `{}` phase with the prior run's context…"

# /export proof-pack
"export.no_release_dir" = "release/ does not exist — the pipeline must reach delivery to generate a proof-pack."
"export.release_empty" = "release/ is empty — the delivery phase must complete to produce proof-pack.zip."
"export.latest_proof_pack" = "[ok] latest proof-pack: {}\n  size: {} KiB\n  path: {}\n\nShare it directly with reviewers. Use `unzip -l {}` to inspect the contents."

# /diff fallback
"diff.not_found" = "Could not find {} — existing artifacts:\n\n"
"diff.no_output_dir" = "  (output/ does not exist yet, run the pipeline first)\n"
"diff.usage" = "\nUsage: /diff prd · /diff architecture · /diff uiux · /diff frontend-notes …"

# /preview
"preview.detected" = "[preview] Detected {}, starting preview…\n  command: `{}` -> {}"
"preview.starting" = "Starting dev server and opening the preview: {}\n  command: `{}`"
"preview.opened" = "Opened the preview: {}\n  (no Run command found; if the page won't open, start the dev server manually)"
"preview.none_yet" = "No previewable frontend yet. Type a requirement and run through the frontend phase (it pauses at the preview gate),\n                   or confirm the base wrote a Preview URL into output/xxx-frontend-notes.md."
"preview.gate_announce" = "[preview] Frontend generated, preview at: {}\n                       Open it in the browser to see the result. Happy? type c to continue (into the backend);\n                       to change it, describe your edits. You can also /preview to auto-start the dev server, /stop-preview to stop."
"preview.gate_no_url" = "Frontend generated, but the base did not record a Preview URL.\n                   Open the project and run the dev server manually, or /preview to try auto-starting it."

# /deploy
"deploy.cli_missing" = "[warn] No deploy CLI detected (vercel/netlify/wrangler). If not installed yet:\n  npm install -g vercel && vercel login, then /deploy.\n  (If already installed, just continue — /deploy runs the deploy command the base recorded.)"
"deploy.cli_ready" = "[ok] Deploy CLI detected, ready to deploy."
"deploy.no_command" = "No deploy command yet. Run through the delivery phase first (the base writes the deploy command into output/xxx-delivery-notes.md), or confirm the base filled in `## Deploy command`."
"deploy.starting" = "Starting deploy, running: `{}`\n  (this may require logging into the platform CLI; follow the prompts)"

# /auto and /manual
"review.auto_on" = "[ok] Autonomous mode: on. Doc and preview checkpoints auto-approve, delivering directly.\n  (to review step by step -> /manual · permanent setting -> auto_approve_gates in .umadevrc)"
"review.manual_on" = "[ok] Review mode: on. Every checkpoint pauses, shows the output, and waits for you to type c to approve or text to revise.\n  (to go fully automatic -> /auto · clarifying questions always ask you first)"

# /animations
"anim.on" = "on"
"anim.off" = "off"
"anim.static" = "static …"
"anim.toggled" = "Animations {} (spinner {})"

# /doctor and picker external-API entry
"doctor.report_written" = "[ok] Diagnostic report written to: {}\n  Send this file to the developers to help us locate the issue."

# Picker first-screen + help overlay (ui.rs)
"picker.logo_subtitle" = "· AI coding project-director Agent"
"picker.tagline" = "  Type a requirement; auto research / design / build / ship"
"picker.group.language" = " Interface language / Language "
"picker.group.host_cli" = " Installed AI tools "
"picker.group.custom_api" = " External model/API "
"picker.group.offline" = " Other "
"picker.engine_title" = " Choose your coding engine "
"help.overlay_subtitle" = " UmaDev — AI coding project-director Agent"

# First-run guided setup (stepped)
"setup.step.language" = "Choose interface language"
"setup.step.base" = "Choose a logged-in base CLI"
"setup.progress" = "Step {} / 3"
"setup.back_hint" = "Esc to go back"
"setup.nav_hint" = "↑↓/jk select · 1-9 quick · Home/End ends · Enter next · Esc back/quit"
"setup.more_above" = "↑ {} more above"
"setup.more_below" = "↓ {} more below"

# Routing memo (conversation context fed to the base, not shown directly)
"run.classified_build_memo" = "(entering the 9-phase pipeline, requirement: {})"

# File checkpoints / phase labels
"checkpoint.phase_label" = "phase start: {}"
"checkpoint.manual_label" = "manual checkpoint"
"checkpoint.created" = "Created file checkpoint `{}`: {}\nUse /rewind to view, /rewind {} to roll back."
"checkpoint.git_required" = "Cannot create a checkpoint — checkpoints snapshot workspace files with a shadow git, which requires git installed locally."
"rewind.empty" = "No file checkpoints yet. /checkpoint [note] makes one manually; one is also created at the start of every phase."
"rewind.list_header" = "File checkpoints (new → old) — use /rewind <id> to roll workspace files back:\n"
"rewind.restored" = "Rolled workspace files back to `{}` (the pre-rewind state was auto-archived, so you can /rewind forward again)."
"rewind.failed" = "Rewind failed: {}"

# /model per-phase tiers (plan / build)
"model.tiers_current" = "Per-phase models — plan/docs: {} · code: {} (/model plan|build <model> to adjust, off to reset)"
"model.tiers_hint" = "Tip: use /model plan <cheaper model> and /model build <stronger model> to assign models per phase — saves time and money."
"model.tiers_busy" = "A run is in progress — model tiers can't be changed now; set them after the current run finishes."
"model.tiers_default" = "default single model"
"model.tiers_default_paren" = "(default single model)"
"model.tiers_updated" = "Model tiers updated — plan/docs phase: {} · code phase: {}\nUse a cheap/fast model for planning and a strong one for code — saves time and money (more so with your own key / a third party)."

# /deploy pre-confirm preflight
"deploy.confirm_preflight" = "Ready to deploy (not run yet). Will run:\n  {}\n\nPre-deploy checklist: 1) build/tests passed  2) env vars and secrets configured on the target platform  3) target environment (prod/preview) correct  4) DB migration / rollback plan ready.\nWhen everything checks out, type /deploy confirm to actually deploy."

# Worker / base spawn error hints (emitted from background tasks, use the process-wide language)
"worker.init_failed" = "[warn] Could not initialize worker `{}`: {}\n  Check: is the custom provider's kind/base_url/key correct? Or use /backend to pick a logged-in CLI; /offline to switch to offline templates."
"pipeline.start_failed" = "[warn] Pipeline failed to start: {}\n  Check: is the working directory writable? Is there enough disk space?"
"worker.timeout" = "Worker `{}` call timed out (5 minutes). Troubleshoot in order:\n  1) run that CLI once in your terminal to confirm it responds;\n  2) if the requirement is large, split it up and retry;\n  3) use /doctor to check worker health, or /offline to temporarily fall back to offline templates."
"worker.not_on_path" = "Worker CLI `{}` is not on PATH.\n  Use /doctor to see which workers are available, or install that CLI and retry;\n  you can also /offline to fall back to offline templates."
"worker.exited" = "The worker process exited abnormally. Check the worker output above to locate the cause;\n  commonly it's not logged in or out of quota — run that CLI once on its own in your terminal to verify."
"pipeline.generic_error" = "The pipeline hit an error. It fell back to offline templates to continue (where applicable). Use /status to see the current state."
"pipeline.error_note" = "[warn] Pipeline error: {}\n  {}"
"base.init_failed" = "[warn] Could not initialize base `{}`: {}\n  Check the current base configuration, or use /backend /offline to switch."
"route.resume_retry" = "[info] Resuming the previous session failed — retried with a fresh session…"
"base.empty_reply" = "[warn] Base `{}` returned no content."
"route.failed" = "[warn] Routing failed (base `{}`): {}\n  You can retry, or use /run <requirement> to start the pipeline explicitly."

# Base-failure classifier — actionable diagnosis (classify -> actionable_message)
"base.fail.auth.claude" = "Base not logged in — run `claude /login` (or check your third-party API key), then /continue"
"base.fail.auth.codex" = "codex not logged in / auth failed — check codex login or API key, then /continue"
"base.fail.auth.opencode" = "opencode not logged in / auth failed — check opencode login or API key, then /continue"
"base.fail.auth.generic" = "Base not logged in / auth failed — check the base login or API key, then /continue"
"base.fail.ratelimit" = "Base hit a rate limit — retry shortly, or switch base (the model is the base's own — set it in the base)"
"base.fail.overloaded" = "Base is overloaded (busy) — retry shortly, or switch base"
"base.fail.network" = "Cannot reach the base model endpoint — check your network or proxy settings"
"base.fail.network.ssl" = "SSL / certificate verification failed — check your proxy or corporate cert (NODE_EXTRA_CA_CERTS)"
"base.fail.context" = "Context exceeded the base limit — /compact to shrink, or switch to a larger-context model"
"base.fail.exited" = "Base process exited (code {})"
"base.fail.turn_failed" = "Base turn failed (no further detail) — retry, or switch base"
"base.fail.idle" = "Base produced no output for {}s with no tool running — it looks hung, or the run budget was reached. While a tool is running UmaDev keeps waiting as long as the base stays alive; for the no-tool idle window raise UMADEV_IDLE_TIMEOUT_SECS, or retry or switch base"

# Preview dev server / deploy (background tasks, use the process-wide language)
"preview.dev_starting" = "[wait] dev server starting, waiting for the port to be ready…"
"preview.dev_ready" = "[ok] dev server ready, browser opened: {}\n  /stop-preview to stop"
"preview.dev_not_ready" = "[warn] dev server not ready within 15s ({}).\n  It may still be compiling, or the port is taken. Open the address manually, or check the host output."
"preview.dev_spawn_failed" = "[warn] Could not start the dev server ({}): {}\n  Run the command manually, then refresh {}"
"preview.port_busy" = "ℹ The port is already in use ({}), probably your existing dev server.\n  Opened the address for you. To restart, first /stop-preview or stop the service holding that port."
"deploy.running" = "[deploy] Deploying, running: `{}` …"
"deploy.login_hint" = "If this is your first deploy, most CLIs need you to log in first: run `vercel login` (or `netlify login`) in a **separate terminal**, then come back and /deploy."
"deploy.done" = "[ok] Deploy complete. {}"
"deploy.done_no_url" = "check the output above for the address"
"deploy.failed" = "[warn] Deploy failed (exit code {}): {}\n  {}"
"deploy.exec_failed" = "[warn] Could not run the deploy command ({}): {}"
"deploy.timeout" = "[warn] Deploy timed out (>5 minutes, aborted). {}\n  Or run it manually in your terminal: `{}`"
"deploy.detected" = "[deploy] Detected deploy target: {} (deploy command auto-selected if the base recorded none)."
"deploy.proof_written" = "[ok] Deploy evidence written: {} (folded into the next proof-pack)."
"deploy.no_target" = "No deploy target detected (no vercel.json / netlify.toml / Dockerfile / fly.toml / built bundle). Add a platform config, or deploy manually."

# --- usage / lessons read-only views (umadev usage · umadev lessons · /usage · /lessons) ---
"usage.empty" = "No usage records yet. After a `run`, per-phase token usage is tracked automatically.\n(UmaDev drives your own base CLI subscription — it never bills you.)"
"usage.title" = "Usage statistics — {} worker calls across {} run(s)"
"usage.run_header" = "Run #{} · base: {}"
"usage.phase_line" = "    {}: {} call(s) · {} tokens"
"usage.run_total" = "  run total: {} call(s) · {} tokens"
"usage.grand_total" = "Grand total: {} tokens"
"usage.cost_estimate" = "Rough cost estimate: ~${} (reference only — blended flat rate, not an invoice)"
"usage.note_combined" = "Note: tokens are the combined input+output reported by the base; the split is not separately recorded."
"lessons.empty" = "Nothing learned yet. As UmaDev hits build/type/dependency/runtime errors during a run, it records them and warns you before the same pitfall bites again."
"lessons.title" = "What UmaDev has learned"
"lessons.efficacy" = "Pitfall self-check — {} total · [ok] validated {} · [warn] still recurring {} · pending {}"
"lessons.top_header" = "High-frequency pitfalls (worst first):"
"lessons.pitfall_line" = "  {} {} (hit {}× · {})"
"lessons.pitfall_fix" = "    avoid: {}"
"lessons.pitfall_ctx" = "    stack: {}"
"lessons.recurring_header" = "Currently steering away from these failed fixes:"
"lessons.avoid_line" = "  · {}"
"lessons.validated_header" = "Validated success patterns (passed the quality gate, reusable):"
"lessons.validated_line" = "  [ok] {} — {}"
"lessons.status.validated" = "validated"
"lessons.status.recurring" = "still recurring"
"lessons.status.active" = "pending"

# --- adopt: brownfield onboarding (umadev adopt · /adopt) ---
"adopt.title" = "UmaDev — existing project adopted (brownfield baseline established)"
"adopt.empty" = "UmaDev — this directory doesn't look like an existing project; a baseline marker was still written for later adoption."
"adopt.workspace" = "  workspace: {}"
"adopt.stack" = "  stack: {}"
"adopt.dev_server" = "  dev server: {}"
"adopt.no_commands" = "  commands: none detected (confirm with the maintainer)"
"adopt.commands_header" = "  commands (use these, don't invent new ones):"
"adopt.command_line" = "    - {}: `{}`"
"adopt.api_count" = "  API contract: {} endpoint(s) reverse-derived from real source (written to .umadev/contracts/ as the adopted baseline)"
"adopt.index_count" = "  source index: {} file(s) · {} chunk(s) (written to .umadev/project-source-index/)"
"adopt.artifacts_header" = "  written:"
"adopt.artifact_line" = "    - {}"
"adopt.notes_header" = "  skipped (fail-open, other steps unaffected):"
"adopt.note_line" = "    · {}"
"adopt.next_steps" = "Next: describe your requirement as usual — UmaDev will make incremental changes, not a rewrite. See UMADEV.md for boundaries + commands."
"adopt.tui_running" = "Adopting the current workspace (detect stack / index source / derive contract)…"

# --- runtime proof (umadev verify --runtime) ---
"runtime.header" = "Runtime evidence"
"runtime.running" = "Booting the dev server and probing routes (proving the app actually runs)…"
"runtime.written" = "  Runtime evidence written: {} (folded into the proof-pack)"
"runtime.write_failed" = "  Runtime evidence write failed (fail-open, not blocking): {}"
"runtime.dev_server" = "dev server: {}"
"runtime.base_url" = "base URL: {}"
"runtime.verified" = "Verified: app booted and answered (ready in {})"
"runtime.routes" = "Route probes: {}/{} route(s) answered"
"runtime.no_response" = "no response"
"runtime.not_verified" = "Runtime not verified (fail-open): {}"
"runtime.e2e_passed" = "e2e passed: {} ({}ms)"
"runtime.e2e_failed" = "e2e failed: {} ({}ms)"

# review report (PR-ready checklist + security scan)
"review.scanning" = "Running pre-PR security scan (secrets + dependencies; installed tools only, fail-open)..."
"review.written" = "Wrote PR review report: {} (folded into the proof-pack)"
"review.write_failed" = "Review report write failed (fail-open, non-blocking): {}"
"review.mergeable" = "No blocking issues from automated checks — ready for human review."
"review.blocked" = "Blocking issue(s) detected — resolve the failing checks before merge."
"pr.scanning" = "PR mode: building the PR body from this run's evidence (review report + proof-pack)…"
"pr.body_written" = "Wrote PR body: {}"
"pr.body_write_failed" = "PR body write failed (fail-open, non-blocking): {}"
"pr.readiness" = "Readiness checks:"
"pr.plan" = "Plan: commit on `{}` → PR into `{}` ({})."
"pr.plan_new_branch" = "creating a new feature branch — never committing on the default branch"
"pr.plan_reuse_branch" = "reusing your current feature branch"
"pr.dry_run" = "Dry run — wrote the PR body and printed the plan, but opened nothing. Re-run with `umadev pr --create` to open the PR."
"pr.branching" = "Creating feature branch `{}` (never committing on the default branch)…"
"pr.committing" = "Staging + committing the run's output…"
"pr.pushing" = "Pushing `{}` to origin (plain push, never force)…"
"pr.opening" = "Opening the PR with `gh pr create`…"
"pr.opened" = "PR opened: {}"
"pr.review_loop" = "Got review feedback? Re-run a single phase with `umadev redo <phase>`, then `umadev pr --create` again to update the branch."
"pr.create_failed" = "`gh pr create` failed (fail-open): {}"
"pr.git_failed" = "git `{}` failed (fail-open): {}"
"pr.fallback" = "Could not finish automatically — falling back to the manual steps below. Your repo is left as it was (no force-push, no rewritten commits)."

# auto mode — clarify skip + per-path gate auto-approval announcement
"auto.clarify_skipped" = "[auto] Skipped clarifying questions automatically (using the original requirement as-is). Switch to /mode guarded if you'd rather answer them."
"auto.gate_approved" = "[auto] Auto-approved checkpoint `{}` — driving the pipeline forward…"

# failure diagnosis — root-cause guess + concrete next step (appended to errors)
"diag.rate_limit" = "Likely cause: the base hit a rate limit / quota. Wait a bit, or check your plan; then /redo to re-run."
"diag.not_logged_in" = "Likely cause: the base isn't logged in (or its key is invalid). Run that base's CLI once in your terminal to log in, then `umadev doctor` to confirm, then /redo."
"diag.network" = "Likely cause: the network is unreachable or the provider is down. Check your connection / proxy, then /redo."
"diag.timeout" = "Likely cause: the base took too long (big task or a slow connection). Try /redo; if it keeps timing out, simplify the requirement or run `umadev doctor`."
"diag.base_missing" = "Likely cause: the base CLI isn't installed or isn't on PATH. Run `umadev doctor` to check, install the base, then /redo."
"diag.generic" = "Run `umadev doctor` to diagnose the base, then /redo to re-run this phase once it's healthy."
"diag.empty_body" = "[warn] {} phase: the base returned an EMPTY reply — this artifact is an OFFLINE SKELETON, not a real generation. Once the base is healthy, run /redo to get the real output."
"diag.transient_retry" = "[warn] Worker call failed transiently ({} phase: {}); retrying in {}ms ({}/{})… ({})"
"diag.call_failed" = "[warn] {} phase: base call failed ({}) — falling back to an OFFLINE SKELETON for this phase (not a real generation). {}"

# quality gate — inline score + findings instead of pointing at the JSON
"quality.gate_unreadable" = "[warn] Quality gate file was written but can't be read back ({}) — treating as not-passed to avoid masking a write failure."
"quality.gate_blocked" = "[warn] Quality gate did NOT pass (score {}/100) — blocking delivery (UD-EVID-003). Fix the items below, then:\n  /redo to re-run the whole pipeline\n  or /continue after fixing to resume"
"quality.top_findings_header" = "Top findings to fix:"

# ── TUI chrome (ui.rs / app.rs) — migrated out of hard-coded strings ──
# Tiny-terminal guard card
"tui.too_small.title" = "Terminal too small"
"tui.too_small.resize" = "Resize to at least {}×{}"
"tui.too_small.now" = "now {}×{}"
# Title row + gate block
"tui.title.workspace_placeholder" = "<workspace>"
"tui.gate_block.title" = "GATE — pause & decide"
"tui.gate_block.hint" = "/continue to approve · type a revision · Esc to quit"
# Transcript scroll indicators (counts are placeholders; arrows are symbols)
"tui.scroll.both" = "↑ {} · ↓ {} more — End to bottom"
"tui.scroll.above" = "↑ {} lines above"
# Toast after a drag-select copy ({} = number of characters copied to clipboard)
"tui.copied" = "Copied {} characters"
# Tool-call rows: merged read/grep batch headline + grep metric
"tui.tool.batch" = "inspected {} files"
"tui.tool.matches" = "{} matches"
# Dim tag on a tool row settled by an interrupt (run aborted/cancelled before
# the tool's result arrived) — spinner stops, row reads as stopped, not success.
"tui.tool.aborted" = "[aborted]"
# Long-output folding: collapsed-summary line + the per-message expand hint.
# The hint names the GLOBAL reveal-all key (Ctrl+O) so a folded row that is not
# the most recent (which Ctrl+R toggles) still advertises a working gesture.
"tui.fold.collapsed" = "… {} more lines · {}"
"tui.fold.expand_hint" = "Ctrl+O expand all"
# Collapsed `[thinking]` reasoning block: the expand-affordance appended to the
# `[thinking] thinking… · 4.2s` header so the user knows the base's chain of
# thought is foldable (the global Ctrl+O verbose toggle reveals the reasoning).
"tui.thinking.expand_hint" = "Ctrl+O to expand reasoning"
# Folded diff-card header: file (+added −removed) · expand hint
"tui.diff.collapsed" = "{} (+{} −{}) · {}"
# Truncation tail when an expanded diff exceeds the per-card line cap
"tui.diff.truncated" = "… {} more lines (open the file to view)"
# Prompt meta-row hints
"tui.hint.palette" = "↑↓ choose · Tab autocomplete · Enter run"
"tui.hint.gate_tag" = "[gate] gate `{}`"
"tui.hint.gate_action" = "· type revision or /continue"
"tui.hint.multiline" = "multi-line · Enter submit · Shift+Enter newline"
"tui.hint.typed" = "Enter submit · Shift+Enter newline · ↑↓ recall"
"tui.hint.finished" = "[ok] complete — new requirement to start again"
"tui.hint.running" = "[wait] pipeline running — wait for the next gate"
"tui.hint.idle" = "Enter submit · /help for all commands"
"tui.hint.aborted" = "[aborted] this round stopped — type a new requirement to start again"
# Slash-command palette popover title
"tui.palette.title" = "slash commands"
# @-file-mention typeahead popover title + prompt hint
"tui.mention.title" = "files"
"tui.hint.mention" = "↑↓ choose · Tab/Enter insert · Esc close"
# Status row
"tui.status.complete" = "[ok] complete"
# Scrollable content overlay progress
"tui.overlay.empty" = "(empty)"
"tui.overlay.progress" = " {}-{} of {} · {}% "
"tui.overlay.scroll_hint" = "↑↓/PgUp PgDn scroll"
# Help overlay — headers
"tui.help.header_picker" = " Help — first-launch picker "
"tui.help.header_chat" = " Help — slash commands & keys "
"tui.help.scroll_hint" = "scroll"
# Help overlay — group titles
"tui.help.group.navigation" = "Navigation"
"tui.help.group.worker" = "Worker / brain"
"tui.help.group.pipeline" = "Pipeline & gates"
"tui.help.group.ship" = "Ship it"
"tui.help.group.inspect" = "Design & inspect"
"tui.help.group.system" = "UI & settings"
"tui.help.group.session" = "Session & exit"
"tui.help.group.editing" = "Keys"
# Help overlay — picker navigation rows
"tui.help.nav.move" = "navigate the worker list"
"tui.help.nav.confirm" = "confirm — saves to ~/.umadev/config.toml"
"tui.help.nav.toggle" = "toggle this help"
"tui.help.nav.quit" = "quit"
# Help overlay — worker group dynamic rows
"tui.help.worker.offline" = "deterministic templates — internal fallback (demo / CI), not a real base"
"tui.help.worker.model" = "set the model id"
# Help overlay — pipeline & gates
"tui.help.pipe.enter" = "submit; non-slash = requirement (or revision at gate)"
"tui.help.pipe.continue" = "approve the active gate"
"tui.help.pipe.revise" = "stay at gate, request changes"
"tui.help.pipe.manual" = "review each checkpoint (pause to approve)"
"tui.help.pipe.auto" = "auto-approve checkpoints (autonomous)"
"tui.help.pipe.diff" = "show an artifact (default: PRD)"
"tui.help.pipe.run" = "start a new run"
"tui.help.pipe.quick" = "lightweight track for a trivial change"
"tui.help.pipe.redo" = "re-run all, or one phase (e.g. /redo frontend)"
"tui.help.pipe.rewind" = "list/rewind file checkpoints"
"tui.help.pipe.init" = "write umadev.yaml"
# Help overlay — ship it
"tui.help.ship.preview" = "start the dev server + open the browser"
"tui.help.ship.stop_preview" = "stop the running preview server"
"tui.help.ship.deploy" = "run the deploy command → live URL"
"tui.help.ship.pr" = "open a GitHub PR (review report + proof-pack body)"
"tui.help.ship.export" = "export the latest proof-pack"
# Help overlay — design & inspect
"tui.help.inspect.design" = "pick a design system"
"tui.help.inspect.template" = "pick a seed template"
"tui.help.inspect.status" = "detailed pipeline status"
"tui.help.inspect.pitfalls" = "self-learning pitfalls knowledge base"
"tui.help.inspect.runs" = "run history + phase timing"
"tui.help.inspect.knowledge" = "list knowledge + design files"
"tui.help.inspect.mcp" = "manage MCP servers"
"tui.help.inspect.skill" = "manage skill packages"
"tui.help.inspect.usage" = "worker-call usage statistics"
"tui.help.inspect.spec" = "show spec clauses"
"tui.help.inspect.verify" = "workspace conformance"
"tui.help.inspect.config" = "all current configuration"
"tui.help.inspect.doctor" = "self-test"
"tui.help.inspect.history" = "conversation history"
"tui.help.inspect.sessions" = "list saved chats you can resume"
"tui.help.inspect.resume" = "reopen a saved chat"
"tui.help.inspect.version" = "versions"
"tui.help.inspect.changelog" = "CHANGELOG"
"tui.help.inspect.bug" = "collect diagnostics to report a bug"
# Help overlay — editing & exit
"tui.help.edit.newline" = "insert newline (multi-line requirement / revision)"
"tui.help.edit.recall" = "recall input history"
"tui.help.edit.autocomplete" = "autocomplete from slash palette"
"tui.help.edit.compact" = "summarize-and-fold the chat to free up context"
"tui.help.edit.expand" = "fold / unfold the latest long reply or tool output"
"tui.help.key.expand_all" = "reveal / hide ALL collapsed output at once"
"tui.help.edit.clear" = "clear chat history (config stays)"
"tui.help.edit.help" = "this overlay"
"tui.help.edit.quit" = "exit"
"tui.help.edit.toggle" = "toggle this overlay"
"tui.help.edit.esc" = "close overlay / quit"
# Slash-command descriptions that the help overlay + palette share (one source).
# Most commands reuse the tui.help.* strings above; these cover the verbs that
# previously had only an English palette hint (or none) and are now registry-owned.
"tui.cmd.claude" = "switch the worker to Claude Code"
"tui.cmd.codex" = "switch the worker to Codex"
"tui.cmd.opencode" = "switch the worker to OpenCode"
"tui.cmd.goal" = "set a goal — keep the worker going until it's met"
"tui.cmd.plan" = "show / steer the live plan"
"tui.cmd.checkpoint" = "snapshot workspace files"
"tui.cmd.cancel" = "stop the running pipeline / agentic round"
"tui.cmd.adopt" = "adopt an existing project (detect stack, index source, derive contract)"
"tui.cmd.mode" = "trust tier: plan / guarded / auto"
"tui.cmd.lessons" = "what UmaDev has learned (pitfalls + proven patterns)"
"tui.cmd.team" = "your AI development team — roster + this run's verdicts & deliverables"
"tui.cmd.lang" = "switch UI language"
"tui.cmd.setup" = "re-open the first-run guide (language + base picker)"
"tui.cmd.guide" = "back to the onboarding guide (alias of /setup)"
"tui.cmd.animations" = "toggle spinner animation on / off"
"tui.cmd.mouse" = "toggle mouse-wheel scrolling (off = native text selection)"
"tui.cmd.redraw" = "force a full repaint (clear stale / garbled cells)"
# Event-stream render (app.rs apply_engine)
"event.phase_done" = "[ok] {}"
"event.verify_started" = "[verify] verify [{}] running: {}"
"event.verify_skipped" = "[skip] verify [{}] skipped — {}"
"event.verify_passed" = "[ok] verify [{}] passed in {}.{}s"
"event.verify_failed" = "[fail] verify [{}] FAILED (exit {}): {}\n  {}"
"event.subtask_started" = "▸ {} subtask `{}` started: {}"
"event.subtask_completed" = "{} {} subtask `{}` {}"
"event.subtask_done" = "done"
"event.subtask_failed" = "failed"
# Agentic markers (app.rs)
"agentic.inspecting" = "[agentic] inspecting the repo…"
"agentic.working_on" = "[agentic] working on: {}"
"agentic.done" = "[agentic] done."
# Slash-command inline pushes (app.rs)
"slash.history_cleared" = "history cleared."
"slash.gate_approved" = "[ok] approved gate `{}` — continuing…"
"slash.mcp_header" = "[package] MCP Servers:\n\n{}"
"slash.skill_header" = "[skill] Skills:\n\n{}"
"slash.mouse_on" = "mouse-wheel scrolling on — Shift+drag for native text selection"
"slash.mouse_off" = "mouse-wheel scrolling off — native text selection restored"
"slash.redraw" = "screen repainted — cleared any stale or garbled cells"
# /spec overlay title
"spec.overlay_title" = " UMADEV_HOST_SPEC_V1 — press Esc to close, ↑↓ scroll "
# /doctor overlay body + title
"doctor.heading" = "Doctor"
"doctor.binary" = "binary       umadev {} (spec {})"
"doctor.workspace" = "workspace    {}"
"doctor.worker" = "worker       {}"
"doctor.manifest_present" = "manifest     umadev.yaml present (level {}, profile {})"
"doctor.manifest_missing" = "manifest     [warn] no umadev.yaml — type /init to create one"
"doctor.worker_availability" = "worker availability:"
"doctor.probing" = "  (probing…)"
"doctor.design_infra" = "design infrastructure:"
"doctor.no_design_systems" = "  [warn] no design systems found in knowledge/design-systems/"
"doctor.templates_label" = "  templates: "
"doctor.knowledge_base" = "knowledge base:"
"doctor.expert_roles" = "  [ok] {} expert roles: {}"
"doctor.no_experts" = "  [warn] no knowledge/experts/ directory"
"doctor.knowledge_files" = "  [ok] {} knowledge files total"
"doctor.project_config" = "project config:"
"doctor.rc_present" = "  [ok] .umadevrc (threshold={}, rounds={})"
"doctor.rc_missing" = "  [pending] no .umadevrc (using defaults)"
"doctor.audit_trail" = "audit trail:"
"doctor.audit_present" = "  [ok] {} ({} entries)"
"doctor.audit_missing" = "  [pending] {} (not yet created)"
"doctor.no_audit" = "  [pending] no audit trail yet (run a pipeline first)"
"doctor.overlay_title" = " doctor — press Esc to close "

# --- Continuous long-session run path (CLI prints, TUI block notes, phase + team review notes) ---
"continuous.session_active" = "Continuous session active ({}) — the default long-session path."
"continuous.session_unavailable" = "  [continuous] session unavailable ({}); falling back to the single-shot per-phase path."
"continuous.auto_gate_resumed" = "[auto] gate {} auto-approved — resuming."
"continuous.lean_complete" = "lean task complete (no release proof-pack — this was a small-scope change)"
"continuous.hardstop_report" = "UmaDev — pipeline stopped before delivery: {}"
"continuous.tui_session_unavailable" = "continuous session unavailable ({}) — set UMADEV_CONTINUOUS=0 to use the per-phase path."
"continuous.block_aborted_busy" = "This round was aborted: this session already has a run holding the workspace (base {}). Wait a moment for the previous round to finish and retry, or /status to see current progress."
"continuous.block_aborted_locked" = "This round was aborted: another run is holding this workspace (base {}).\n{}"
"continuous.block_aborted_io" = "This round was aborted: {} — {}"
"continuous.plan_mode_skip" = "[plan] Plan mode (read-only): spec/frontend/backend not executed — no real code written."
"continuous.phase_failed" = "[fail] {} phase failed: {} — continuous session path stopped."
"continuous.no_source_hardstop" = "[fail] No real source files produced — pipeline stopped, nothing delivered (continuous hard gate)."
"director.run_done" = "UmaDev — the director finished this build."
"director.no_source_hardstop" = "UmaDev — the director reported a build but the workspace has zero real source files: objectively this build is not done (director-path hard gate)."
"continuous.dangerous_action_denied" = "[gate] Irreversible action needs user confirmation; denied this turn: {} -> {}"
"continuous.tool_call_blocked" = "[gov] {} phase: base tool call tripped {} — {} (target: {})"
"continuous.phase_truncated" = "[warn] {} phase was truncated (base hit its turn limit); keeping what was produced, continuing."
"continuous.phase_truncated_degraded" = "[DEGRADED] {} phase was truncated AND its key deliverables are missing ({}); the output is incomplete — the quality/hard gates downstream will catch this. Re-run or /continue to finish."
"continuous.node.docs" = "docs gate"
"continuous.node.preview" = "preview gate"
"continuous.node.quality" = "quality"
"continuous.team.passed_after_rework" = "[team] {} review passed (no blocking items after {} rework round(s)) — proceeding."
"continuous.team.unresolved_advisory" = "[team] {} still has {} blocking item(s) (rework budget reached / no convergence); kept as advisory, proceeding."
"continuous.team.inject_rework" = "[team] {} review raised {} blocking item(s) — injecting a rework directive into the main session (round {})…"
"continuous.team.cross_review_header" = "[team] {} role-team cross-review (read-only forks, parallel): {} critic(s) each review from their own seat…"
"continuous.team.seat_passed" = "[team] {}: passed, no blocking items."
"continuous.team.seat_blocking" = "[team] {}: raised {} blocking item(s)."
"continuous.verify_failed" = "[verify] Real build/test/lint had failing step(s) (recorded to the audit; the quality gate scores against it)."
"continuous.quality_gate_result" = "Quality gate result: {}/100 · {} (deterministic hard signal)"
"continuous.quality_gate_advisory" = "Quality score: {}/100 — advisory suggestions (a lean task; not a blocker)"
"continuous.quality_gate_findings" = "Quality gate top findings:\n{}"
"continuous.quality_gate_blocked" = "[fail] Quality gate failed ({}/100) — pipeline stopped at quality, not delivered (continuous deterministic hard gate)."
"continuous.governance_catchup" = "[gov] {} real file(s) have governance violations (base has no real-time hook; caught by post-write scan), reworking…"
"continuous.governance_clean" = "[gov] Governance violations fixed."
"continuous.governance_remaining" = "[gov] {} violation(s) remain (the quality gate will catch them again)."
"continuous.governance_rework_intro" = "These real source files have governance violations (emoji-as-icon / hardcoded colors / AI-slop). Fix only these: replace hardcoded colors with design tokens, replace emoji with an icon library (Lucide / Heroicons / Tabler), strip AI-template filler; change nothing else."
